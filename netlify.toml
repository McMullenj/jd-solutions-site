[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm build:site"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
